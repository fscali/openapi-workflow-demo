openapi: 3.0.3
info:
  title: Payment API
  version: 1.0.0
paths:
  /payments:
    post:
      summary: Create a payment
      x-policies:
        - $ref: "../policies/rate-limit.yaml"
        - $ref: "../policies/jwt-auth.yaml"
      responses:
        "200":
          description: OK
